[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Add the backend directory (where this file lives) to sys.path so that
# tests can import application modules like `database`, `models`, `main`
# regardless of how pytest is invoked or which pytest version is used.
pythonpath = .